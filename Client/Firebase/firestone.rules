/**
 * @file firestone.rules
 * @description Firestore security rules for AttendanceX. Define read/write permissions for attendance data.
 */

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}